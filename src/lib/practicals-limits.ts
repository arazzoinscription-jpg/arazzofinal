/**
 * Limites de dépôt des travaux pratiques par élève et par leçon.
 * Module client-safe (aucune dépendance serveur) : réutilisé par l'action
 * serveur `recordPractical` et par le composant élève `LessonPractical`.
 */
export const MAX_PRACTICAL_PHOTOS = 3;
export const MAX_PRACTICAL_VIDEOS = 2;
